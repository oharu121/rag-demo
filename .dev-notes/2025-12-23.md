<script lang="ts">
  interface Props {
    size?: 'sm' | 'md' | 'lg' | 'xl';
    className?: string;
  }

  let { size = 'lg', className = '' }: Props = $props();

  const sizeClasses = {
    sm: 'text-2xl',
    md: 'text-4xl', 
    lg: 'text-6xl',
    xl: 'text-8xl'
  };
</script>

<div class="magical-logo-container {className}">
  <!-- Main Logo Text -->
  <h1 class="magical-logo {sizeClasses[size]}">
    <span class="magical-word">
      <span class="letter" style="--delay: 0s">M</span>
      <span class="letter" style="--delay: 0.1s">a</span>
      <span class="letter" style="--delay: 0.2s">g</span>
      <span class="letter" style="--delay: 0.3s">i</span>
      <span class="letter" style="--delay: 0.4s">c</span>
      <span class="letter" style="--delay: 0.5s">a</span>
      <span class="letter" style="--delay: 0.6s">l</span>
    </span>
    <span class="magical-word">
      <span class="letter sparkle-letter" style="--delay: 0.8s">P</span>
      <span class="letter" style="--delay: 0.9s">r</span>
      <span class="letter" style="--delay: 1.0s">o</span>
      <span class="letter" style="--delay: 1.1s">m</span>
      <span class="letter" style="--delay: 1.2s">p</span>
      <span class="letter" style="--delay: 1.3s">t</span>
    </span>
  </h1>

  <!-- Magical Particles -->
  <div class="particles">
    {#each Array(12) as _, i}
      <div 
        class="particle" 
        style="--i: {i}; --x: {Math.random() * 100}%; --y: {Math.random() * 100}%"
      ></div>
    {/each}
  </div>

  <!-- Magical Orbs -->
  <div class="orbs">
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>
  </div>

  <!-- Shine Effect -->
  <div class="shine-overlay"></div>
</div>

<style>
  .magical-logo-container {
    position: relative;
    display: inline-block;
    padding: 2rem;
    overflow: hidden;
  }

  .magical-logo {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    position: relative;
    z-index: 2;
  }

  .magical-word {
    display: flex;
    align-items: center;
    gap: 0.05em;
  }

  .letter {
    display: inline-block;
    background: linear-gradient(
      135deg,
      hsl(262 83% 58%) 0%,
      hsl(280 100% 70%) 25%,
      hsl(300 100% 80%) 50%,
      hsl(45 100% 70%) 75%,
      hsl(262 83% 58%) 100%
    );
    background-size: 400% 400%;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: 
      shimmer 4s ease-in-out infinite,
      float 6s ease-in-out infinite,
      letterGlow 3s ease-in-out infinite;
    animation-delay: var(--delay);
    text-shadow: 0 0 30px rgba(167, 139, 250, 0.5);
    filter: drop-shadow(0 0 10px rgba(167, 139, 250, 0.3));
  }

  .sparkle-letter {
    position: relative;
  }

  .sparkle-letter::before {
    content: '✨';
    position: absolute;
    top: -20%;
    right: -10%;
    font-size: 0.5em;
    animation: sparkleRotate 2s linear infinite;
    filter: hue-rotate(45deg);
  }

  .sparkle-letter::after {
    content: '⭐';
    position: absolute;
    bottom: -20%;
    left: -10%;
    font-size: 0.3em;
    animation: sparkleRotate 3s linear infinite reverse;
    filter: hue-rotate(90deg);
  }

  /* Magical Particles */
  .particles {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
  }

  .particle {
    position: absolute;
    width: 4px;
    height: 4px;
    background: radial-gradient(circle, hsl(45 100% 70%), transparent);
    border-radius: 50%;
    left: var(--x);
    top: var(--y);
    animation: particleFloat 8s ease-in-out infinite;
    animation-delay: calc(var(--i) * 0.5s);
    opacity: 0.6;
  }

  /* Magical Orbs */
  .orbs {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 0;
  }

  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(1px);
    opacity: 0.4;
  }

  .orb-1 {
    width: 80px;
    height: 80px;
    background: radial-gradient(circle, hsl(262 83% 58%), transparent);
    top: 20%;
    left: 10%;
    animation: orbFloat 10s ease-in-out infinite;
  }

  .orb-2 {
    width: 60px;
    height: 60px;
    background: radial-gradient(circle, hsl(280 100% 70%), transparent);
    top: 60%;
    right: 15%;
    animation: orbFloat 12s ease-in-out infinite reverse;
  }

  .orb-3 {
    width: 40px;
    height: 40px;
    background: radial-gradient(circle, hsl(45 100% 70%), transparent);
    bottom: 20%;
    left: 20%;
    animation: orbFloat 8s ease-in-out infinite;
    animation-delay: 2s;
  }

  /* Shine Overlay */
  .shine-overlay {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(255, 255, 255, 0.1) 45%,
      rgba(255, 255, 255, 0.3) 50%,
      rgba(255, 255, 255, 0.1) 55%,
      transparent 100%
    );
    animation: shineSlide 6s ease-in-out infinite;
    z-index: 3;
    pointer-events: none;
  }

  /* Animations */
  @keyframes shimmer {
    0%, 100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px) scale(1);
    }
    33% {
      transform: translateY(-5px) scale(1.02);
    }
    66% {
      transform: translateY(-2px) scale(0.98);
    }
  }

  @keyframes letterGlow {
    0%, 100% {
      filter: drop-shadow(0 0 10px rgba(167, 139, 250, 0.3));
    }
    50% {
      filter: drop-shadow(0 0 20px rgba(167, 139, 250, 0.6));
    }
  }

  @keyframes sparkleRotate {
    from {
      transform: rotate(0deg) scale(1);
      opacity: 0.7;
    }
    50% {
      transform: rotate(180deg) scale(1.2);
      opacity: 1;
    }
    to {
      transform: rotate(360deg) scale(1);
      opacity: 0.7;
    }
  }

  @keyframes particleFloat {
    0%, 100% {
      transform: translateY(0px) translateX(0px) scale(1);
      opacity: 0.6;
    }
    25% {
      transform: translateY(-20px) translateX(10px) scale(1.2);
      opacity: 1;
    }
    50% {
      transform: translateY(-10px) translateX(-5px) scale(0.8);
      opacity: 0.4;
    }
    75% {
      transform: translateY(-30px) translateX(15px) scale(1.1);
      opacity: 0.8;
    }
  }

  @keyframes orbFloat {
    0%, 100% {
      transform: translate(0px, 0px) scale(1);
      opacity: 0.4;
    }
    33% {
      transform: translate(20px, -15px) scale(1.1);
      opacity: 0.6;
    }
    66% {
      transform: translate(-10px, 10px) scale(0.9);
      opacity: 0.3;
    }
  }

  @keyframes shineSlide {
    0% {
      left: -100%;
    }
    50% {
      left: 100%;
    }
    100% {
      left: -100%;
    }
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .magical-logo-container {
      padding: 1rem;
    }
    
    .magical-logo {
      gap: 0.25rem;
    }
    
    .orb-1, .orb-2, .orb-3 {
      display: none;
    }
  }

  @media (max-width: 480px) {
    .magical-word {
      gap: 0.02em;
    }
  }
</style>